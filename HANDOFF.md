# Ridgeline 引き継ぎ説明書

## 1. プロジェクト概要

Ridgelineは、神奈川県の病院と看護師採用情報を探すためのローカルWebプロトタイプです。

- ブランド名：Ridgeline
- 技術構成：React 19 / Vite 6
- 登録施設：神奈川県内50施設
- 主な画面：病院一覧、地域・施設形態フィルター、リスト／地図表示、病院詳細、転職サポート
- 地図：各施設のGoogleマップ検索リンクを使用
- 求人：ハローワーク求人情報提供APIの接続準備済み
- 公開方針：ローカル専用。所有者の明示的な許可なしに公開・デプロイしないこと

## 2. 重要なデザイン・仕様決定

- ヘッダーは文字の「Ridgeline」のみ。フクロウを含む追加ロゴは置かない。
- 病院検索の条件は「勤務地」と「施設形態」の2項目のみ。
- 「情報確認」「指定区分」「外観画像」の検索項目は追加しない。
- メディズバンクの名称は表示しない。
- 転職サポートのヒーローでは、同梱されている看護師のバストアップ画像を右側に自然に配置する。
- 全体は清潔感があり、編集的で少し上質な印象にする。派手な装飾は避ける。
- 病院外観画像が未確認の施設は、画像を推測で掲載せず「確認中」と表示する。

詳細な継続指示は `AGENTS.md`、既存の確認記録は `design-qa.md` を参照してください。

## 3. 起動方法

Node.js 20以降を推奨します。展開した `ridgeline` フォルダで次を実行します。

```bash
npm install
npm run dev
```

表示されたローカルURL（通常は `http://localhost:5173/`）をブラウザで開きます。

ビルドとテスト：

```bash
npm run build
npm run test:sites
```

## 4. 主なファイル

- `src/App.jsx`：病院検索、一覧、地図、詳細画面
- `src/data.js`：50施設の基本情報とGoogleマップリンク生成
- `src/CareerSupport.jsx`：転職サポート画面
- `src/HelloWorkJobs.jsx`：ハローワーク求人表示UI
- `src/styles.css`：全体のスタイル
- `public/assets/`：病院画像、転職サポート画像、人物画像
- `worker/index.js`：ハローワーク連携を含むサーバー側処理
- `.env.example`：API接続設定のひな形（実際の認証情報は未同梱）

## 5. ハローワーク連携

求人情報を実際に取得するには、ハローワーク求人情報提供サービスの利用申請・承認と接続情報が必要です。`.env.example` を `.env` にコピーし、発行された値を設定します。

```bash
cp .env.example .env
```

認証情報をソースコード、会話、Git、ZIPへ書き込まないでください。未設定の場合、画面は「連携準備中」と表示します。

## 6. データと画像に関する注意

- 病院名、住所、採用条件、URL、募集状況は変更される可能性があります。公開利用前に各病院の公式サイトと公的情報で再確認してください。
- 登録情報はプロトタイプ用であり、神奈川県内の全医療機関を網羅することを保証するものではありません。
- 一部の外観画像は、公式サイトの外観を参考にした独自生成画像です。実在施設の公式写真そのものではありません。
- `public/assets/brand-model/ridgeline-nurse-bust.png` は所有者が提供した人物写真をもとにした素材です。第三者への共有・公開・二次利用は、本人および権利者の許可範囲内で行ってください。

## 7. 別のCodexで作業を続ける場合

1. このZIPを展開する。
2. 展開した `ridgeline` フォルダをCodexのワークスペースとして開く。
3. Codexへ次のように依頼する。

> `HANDOFF.md` と `AGENTS.md` を最初にすべて読み、現在の仕様とローカル専用方針を守ってRidgelineの作業を続けてください。変更前に既存実装を確認し、変更後はローカルブラウザで動作確認してください。

## 8. ZIPに含めていないもの

- `node_modules/`：展開後に `npm install` で復元
- `dist/`：`npm run build` で再生成
- `.git/`：この端末のGit履歴・設定
- `work/`：作業用ファイル
- `.env` などの秘密情報

